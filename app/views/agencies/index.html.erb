<p id="notice"><%= notice %></p>

<h1>Listing Agencies and Services</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>City</th>
      <th>Services</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @agencies.each do |agency| %>
      <tr>
        <td><%= agency.name %></td>
        <td><%= agency.city %></td>
        <td><% agency.service_offerings.each do |so| %>
            <%= so.name %><br>
            <% end %>
            </td>
        </td>
        <td><%= link_to 'Show', agency %></td>
        <td><%= link_to 'Edit', edit_agency_path(agency) %></td>
        <td><%= link_to 'Destroy', agency, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create New Agency', new_agency_path %> |
<%= link_to 'Service Offerings List', service_offerings_path %>
