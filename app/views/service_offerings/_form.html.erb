<%= form_for(@service_offering) do |f| %>
  <% if @service_offering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_offering.errors.count, "error") %> prohibited this service_offering from being saved:</h2>

      <ul>
      <% @service_offering.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
